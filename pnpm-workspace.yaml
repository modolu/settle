# pnpm 10 blocks dependency postinstall scripts unless allowed here.
# unrs-resolver (pulled in by eslint-import-resolver-typescript) ships an
# optional native binary and falls back to JS when its build is skipped.
allowBuilds:
  unrs-resolver: false
